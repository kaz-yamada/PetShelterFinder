<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/default_margin"
    android:orientation="horizontal">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/shelter_name_text_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/text_margin"
            android:textSize="@dimen/heading_text_size"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/shelter_address_text_view"
            style="@style/TextLargeBody"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
    </LinearLayout>

    <ImageView
        android:layout_width="@dimen/arrow_drawable_size"
        android:layout_height="@dimen/arrow_drawable_size"
        android:layout_gravity="center"
        android:contentDescription="@string/view_shelter_details"
        app:srcCompat="@drawable/ic_chevron_right" />
</LinearLayout>
